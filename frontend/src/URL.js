const REMOTE_URL = "ec2-52-66-237-98.ap-south-1.compute.amazonaws.com";
const LOCAL_URL = "http://localhost";
const useLocal = true;
const URL = useLocal ? LOCAL_URL : REMOTE_URL;
export default URL;
